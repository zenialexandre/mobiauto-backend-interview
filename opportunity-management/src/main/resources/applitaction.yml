server:
  port: 8080
spring:
  application:
    name: opportunity-management
  datasource:
    username: admin
    password: admin_password
    url: jdbc:postgresql://localhost:5432/opportunity-management
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
    database-platform: org.hibernate.dialect.PostgreSQLDialect
